name: Bridge Check

on:
  push:
    paths:
      - ".github/workflows/bridge-check.yml"
      - "coderover-bridge/**"
  pull_request:
    paths:
      - ".github/workflows/bridge-check.yml"
      - "coderover-bridge/**"

jobs:
  bridge-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: coderover-bridge/package-lock.json

      - name: Install bridge dependencies
        working-directory: coderover-bridge
        run: npm ci

      - name: Run bridge tests
        working-directory: coderover-bridge
        run: npm test

      - name: Verify bridge entrypoints load
        working-directory: coderover-bridge
        run: |
          node -e "const mod = require('./dist'); const names = ['startBridge', 'openLastActiveThread', 'watchThreadRollout']; for (const name of names) { if (typeof mod[name] !== 'function') { throw new Error('Missing export: ' + name); } }"
